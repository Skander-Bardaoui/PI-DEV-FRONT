// src/utils/business-info.utils.test.ts

import { describe, it, expect } from 'vitest';

describe('business-info.utils', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
